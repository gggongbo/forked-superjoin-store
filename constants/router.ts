const router = {
  Login: '/login',
  MakeOffer: '/makeoffer',
  Offer: '/offer',
  //   Calendar: '/calendar', Holding: calendar logic
  Customer: '/customer',
  Reward: '/reward',
  Support: '/support',
};

type Router = typeof router;

export default router as Router;
