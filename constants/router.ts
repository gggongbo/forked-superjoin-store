const router = {
  Login: '/login',
  CreateCall: '/createCall',
  Call: '/call',
  Member: '/member',
  Reward: '/reward',
  Support: '/support',
};

type Router = typeof router;

export default router as Router;
