# lint-every-push.yml

name: 'lint-every-push'

# workflow를 동작하게하는 trigger
# repository에 push 이벤트가 발생할 때마다 실행
on: push

jobs:
  test:
    name: Lint Every Push
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Use Node.js
        uses: actions/setup-node@v2
        with:
          node-version: ${{ secrets.NODE_VERSION }}

      - run: yarn lint
